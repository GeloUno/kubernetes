apiVersion: v1
kind: Service
metadata:
  name: {{ include "gk-web.fullname" . }}
  labels:
    {{- include "gk-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gk-web.selectorLabels" . | nindent 4 }}
