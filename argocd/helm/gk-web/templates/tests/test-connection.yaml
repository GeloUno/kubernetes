apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gk-web.fullname" . }}-test-connection"
  labels:
    {{- include "gk-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gk-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
